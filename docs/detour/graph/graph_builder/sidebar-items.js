initSidebarItems({"fn":[["determine_splits","Determine where the cluster should split. Based on the original temporal bounds of the bounding boxes in the cluster and the splits required to attain temporal monotonicity. Note: the required splits from `get_temporal_splits` starts with a t2, i.e. the original must have the earliest timestamp."],["get_graph","Constructs the graph Handles spatial node clustering, edge clustering and detecting less frequently visited places."],["get_temporal_splits","Determines where the cluster representative bbox should split"],["merge_nodes",""],["reassign_edges",""],["split_bbox",""]]});